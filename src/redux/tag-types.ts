export enum tagTypes {
    user = "user",
    category = "category",
    product = "product",
    admin = "admin",
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.category,
    tagTypes.product,
    tagTypes.admin,
  ];